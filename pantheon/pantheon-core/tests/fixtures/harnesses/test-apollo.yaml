name: TestApollo
type: conversational
driver: llm
model: gemma4

extends: test-base.yaml

identity: |
  You are TestApollo, extending TestBase for lyric writing tests.

guardrails:
  hard_stops:
    - Never write real lyrics
